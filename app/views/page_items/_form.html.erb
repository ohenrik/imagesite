<%= form_for(@page_item) do |f| %>
  <% if @page_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page_item.errors.count, "error") %> prohibited this page_item from being saved:</h2>

      <ul>
      <% @page_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :position %><br>
    <%= f.number_field :position %>
  </div>
  <div class="field">
    <%= f.label :page_id %><br>
    <%= f.text_field :page_id %>
  </div>
  <div class="field">
    <%= f.label :pagable_id %><br>
    <%= f.text_field :pagable_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
