<!-- Modal -->
<div class="modal fade" id="mediaModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog media-modal-dialog" id="media-modal-dialog">


    </div>
</div>

<div class="row pages">
    <div class="col-xs-12 new-link">
        <%= link_to new_page_path, class: "btn btn-default pull-right" do %>
            <i class="fa fa-file-text-o"></i> New Page
        <% end %>
    </div>

    <% @pages.each do |page| %>
        <div class="page clearfix">
            
            <div class="col-sm-5 col-md-4 listed-page-thumbnail">
                <%= link_to edit_page_path(page), class: "thumbnail" do %>
                    <% if page.photo %>
                        <%= image_tag page.photo.image_url(:large_thumb).to_s, class: "img-responsive" %>
                    <% else %>
                        <%= image_tag "default/large_thumb_no-thumb.png", class: "img-responsive" %>
                    <% end %>
                <% end %>
            </div>
            
            <div class="clearfix visible-xs"></div>
            <div class="col-sm-7 col-md-8">
                <h2 class="listed-page-header">
                    <%= link_to edit_page_path(page) do %>
                        <i class="fa fa-pencil-square text-muted"></i>
                        <%= page.name %>

                    <% end %>
                </h2>
                <h3 class="listed-page-status text-muted">
                    
                    <small>
                        <i class="fa fa-cogs fa-fw"></i> Settings
                        | 
                        <%= link_to page_path(page), data: { no_turbolink: true }, class: "text-muted" do %>
                            <i class="fa fa-eye fa-fw"></i> Show
                        <% end %>

                        |
                        <%= link_to page, method: :delete, data: { confirm: 'Are you sure?' }, class: "text-muted" do %>
                             <i class="fa fa-trash-o"></i> Delete
                        <% end %>

                        |
                        <%= link_to thumbnail_modal_path(:item => page.class, :item_id => page.id), remote: true, class: "text-muted" do %>
                            <i class="fa fa-picture-o fa-fw"></i> Thumbnail
                        <% end %>

                        <% if page != current_tenant.home %>
                            |
                            <%= link_to set_home_page_path(page), method: :post, class: "text-muted" do %>
                                <i class="fa fa-home fa-fw"></i> Homepage
                            <% end %>
                        <% end %>

                         | <i class="fa <%= page.status == 'published' ? 'fa-check-square' : 'fa-square-o' %>"></i> <%= page.status.titlecase %>
                    </small>
                </h3>
                <p><%= truncate(page.excerpt, :length => 350) unless page.excerpt.blank? %></p>
            </div>

        </div>
        
    <% end %>

    <% if @pages.empty? %>
        <div class="col-xs-12">
            <%= link_to new_page_path do %>
                <div class="alert alert-info">
                    <p>
                        <strong>No pages created: </strong>To create a new page click here.
                    </p>
                </div>
            <% end %>
        </div>
    <% end %>

</div>

